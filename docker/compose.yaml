version: '3.8'

services:
  agent_api:
    build:  
      context: ..
      dockerfile: docker/Dockerfile
    image: fred_agent:latest
    container_name: fred_agent_api
    ports:
      - "8000:8000"
    env_file: 
      - ../.env
    restart: unless-stopped

